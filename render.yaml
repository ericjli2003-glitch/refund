services:
  - type: web
    name: refund
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: refund-db
          property: connectionString
      - key: SHOPIFY_API_KEY
        sync: false
      - key: SHOPIFY_API_SECRET
        sync: false
      - key: SHOPIFY_APP_URL
        sync: false
      - key: SCOPES
        value: read_orders,write_orders,read_returns,write_returns,customer_read_customers,customer_read_orders,customer_write_orders
      - key: PUBLIC_SUPPORT_EMAIL
        sync: false

databases:
  - name: refund-db
    databaseName: refund
    user: refund
